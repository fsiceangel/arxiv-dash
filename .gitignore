# local tooling / preview
.claude/
__pycache__/
*.pyc

# run logs
harvest.log
update.log

# Kaggle metadata snapshot (5+ GB, not committed)
meta/

# local Task Scheduler wrapper (machine-specific absolute paths)
run_daily.bat
